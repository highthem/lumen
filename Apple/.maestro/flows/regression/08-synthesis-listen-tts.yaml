appId: com.highthem.lumen
tags: [regression, tts, voice]
env:
  SYNTHESIS_DEEPLINK: "lumen://test/synthesis?state=ready"
---
- launchApp:
    clearState: true
    arguments:
      isMaestro: "true"

- openLink: ${SYNTHESIS_DEEPLINK}
- runFlow: ../_shared/confirm-ios-open-lumen.yaml

# Synthesis ready, tap Listen
- assertVisible:
    id: "synthesis-screen"
- tapOn:
    id: "synthesis-listen-button"

# Reading focus active : paragraphe 1 visible, autres dimmed
- assertVisible:
    id: "synthesis-focus"
- assertVisible:
    id: "synthesis-listen-button"

# Pause
- tapOn:
    id: "synthesis-listen-button"
- assertVisible:
    id: "synthesis-listen-button"
