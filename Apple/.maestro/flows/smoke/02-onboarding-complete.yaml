appId: com.highthem.lumen
name: Onboarding Complete
tags: 
  - smoke 
  - onboarding
---
- launchApp:
    clearState: true
    arguments:
      isMaestro: "true"
- runFlow: ../_shared/confirm-ios-open-lumen.yaml

# Welcome
- assertVisible: "Quelques minutes à toi."
- tapOn: "Commencer"

# Pitch
- assertVisible: "Cinq minutes."
- tapOn: "Suivant"

# Permissions
- assertVisible: "On a besoin de deux choses."
- tapOn: "Continuer"
- runFlow: ../_shared/dismiss-system-permission.yaml

# Première alarme
- assertVisible: "À quelle heure veux-tu commencer ?"
- tapOn:
    id: "alarm-time-picker"
- tapOn: "Programmer"

# Land on Dashboard — use id-based selectors (text matching with French
# diacritics is unreliable in Maestro 2.5.1)
- waitForAnimationToEnd:
    timeout: 3000
- extendedWaitUntil:
    visible:
      id: "dashboard-screen"
    timeout: 8000
- assertVisible:
    id: "ritual-cta"
