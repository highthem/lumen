appId: com.highthem.lumen
tags: [edge, lifecycle]
---
- launchApp:
    clearState: true
    arguments:
      isMaestro: "true"
- openLink: "lumen://ritual/q2"
- runFlow: ../_shared/confirm-ios-open-lumen.yaml

- assertVisible:
    id: "energy-orb"

# Background l'app
- pressKey: Home

- waitForAnimationToEnd:
    timeout: 2000

# Reopen
- launchApp:
    stopApp: false

# Should resume on the same ritual step
- extendedWaitUntil:
    visible:
      id: "energy-orb"
    timeout: 3000
